#should be set from while embedded broker is created
config.kafka.bootstrapServers=${spring.embedded.kafka.brokers}
config.kafka.schema-registry-url=http://localhost:8081

# Enables load poc and configures some of the aspects of the test
config.kafka.load.enabled=false

usecase.raw-enabled=true
usecase.spring-string-enabled=false
usecase.spring-avro-enabled=false
usecase.spring-avro-repo-enabled=false
